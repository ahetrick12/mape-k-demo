package src.mapek;

/*
 * Observer interface in observer pattern, used on objects that observe a subject to see changes in some a value(s)
 */
public interface Listener {
    public void update(int num);
}
